import NotFoundState from '@/components/NotFoundState'

const NotFoundPage = () => {
  return (
    <NotFoundState
      title="Página no encontrada"
      description="La URL no existe o ya no está disponible."
    />
  )
}

export default NotFoundPage
